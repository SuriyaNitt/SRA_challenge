#include <iostream>
#include <opencv2/core/core.hpp>

#include <human_detection.hpp>

std::vector<cv::Rect> human_detection(cv::Mat &image) {
    cv::Mat inputImage = image;
    std::cout << "Detecting humans.." << std::endl;

    std::vector<cv::Rect> detections;
    

    return detections;
}

